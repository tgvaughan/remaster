<!-- Basic coalescent inference simulation study -->

<!-- Uses Remaster to simulate a non-ultrametric tree under a coalescent model with
     constant population size 2.0, simulates sequence evolution down the tree at
     a fixed rate of 0.05/site/time then uses BEAST to jointly infer the population
     size and the substitution rate from the resulting sequence alignment. -->

<beast version="2.8"
       namespace="beast.base.spec.inference.parameter:remaster
                  :beast.base.inference
                  :beast.base.evolution.tree
                  :beast.base.evolution.alignment
                  :beast.base.spec.evolution.alignment
                  :beast.base.spec.evolution.likelihood
                  :beast.base.spec.evolution.tree
                  :beast.base.spec.evolution.tree.coalescent
                  :beast.base.spec.evolution.sitemodel
                  :beast.base.spec.evolution.substitutionmodel
                  :beast.base.spec.inference.distribution
                  :beast.base.spec.inference.operator
                  :beast.base.spec.evolution.operator
                  :beast.base.evolution.operator
                  :feast.simulation.SimulatedAlignment">

  <!-- Simulated tree -->
  <tree id="trueTree" spec="SimulatedTree">
    <trajectory id="traj" spec="CoalescentTrajectory">
      <population id="pop" spec="coalescent.ConstantPopulation" popSize="2.0"/>
      <reaction spec="Reaction" rate="100 0" changeTimes="1">0 -> pop</reaction>
    </trajectory>
  </tree>

  <!-- Simulated alignment -->
  <data id="alignment" spec="feast.simulation.SimulatedAlignment" sequenceLength="2000">
    <tree idref="trueTree"/>
    <siteModel spec="SiteModel" mutationRate="0.05">
      <substModel spec="JukesCantor"/>
    </siteModel>
  </data>
  <traitset id="tipDates" spec="feast.fileio.TipDatesFromTree" tree="@trueTree">
    <taxa id="taxonSet" spec="TaxonSet" alignment="@alignment"/>
  </traitset>

  <!-- Inference -->
  <run spec="MCMC" chainLength="1000000">
    <state>
      <stateNode spec="RealScalarParam" id="inferredPopSize" value="1.0"/>
      <stateNode spec="Tree" id="tree">
        <taxonset idref="taxonSet"/>
        <trait idref="tipDates"/>
      </stateNode>
      <stateNode spec="RealScalarParam" id="inferredMutationRate" value="0.1"/>
    </state>

    <distribution id="posterior" spec="CompoundDistribution">

      <distribution id="likelihood" spec="TreeLikelihood">
        <data idref="alignment"/>
        <tree idref="tree"/>
        <siteModel spec="SiteModel" mutationRate="@inferredMutationRate">
          <substModel spec="JukesCantor"/>
        </siteModel>
      </distribution>

      <distribution id="treePrior" spec="coalescent.Coalescent">
        <treeIntervals spec="TreeIntervals" tree="@tree"/>
        <populationModel spec="ConstantPopulation" popSize="@inferredPopSize"/>
      </distribution>

      <distribution id="popSizePrior" spec="Exponential" mean="1" param="@inferredPopSize"/>
      <distribution id="mutationRatePrior" spec="Exponential" mean="1" param="@inferredMutationRate"/>
    </distribution>

    <operator spec="ScaleOperator" id="popSizeScaler" scaleFactor="0.8"
              parameter="@inferredPopSize" weight="1.0"/>
    <operator spec="ScaleOperator" id="mutationRateScaler" scaleFactor="0.8"
              parameter="@inferredMutationRate" weight="1.0"/>

    <operator id='treeIntervalScaler' spec='IntervalScaleOperator' scaleFactor="0.5" tree="@tree" weight="3"/>
    <operator id="treeScaler" spec="beast.base.spec.evolution.operator.UpDownOperator" up="@tree" scaleFactor="0.1" weight="4.0"/>
    <operator id="treeRootScaler" spec="kernel.BactrianScaleOperator" scaleFactor="0.5" tree="@tree" rootOnly="true" weight="3"/>
    <operator id="uniformNodeOperator" spec="kernel.BactrianNodeOperator" tree="@tree" weight="30"/>
    <operator id="subtreeSlide" spec="kernel.BactrianSubtreeSlide" size="1.0" tree="@tree" weight="15"/>
    <operator id="exchangeNarrow" spec="Exchange" isNarrow="true" tree="@tree" weight="15"/>
    <operator id="exchangeWidt" spec="Exchange" isNarrow="false" tree="@tree" weight="3"/>
    <operator id="wilsonBalding" spec="WilsonBalding" tree="@tree" weight="3"/>

    <!-- File log -->
    <logger fileName="$(filebase).log" logEvery="100">
      <log idref="inferredPopSize"/>
      <log idref="inferredMutationRate"/>
    </logger>

    <logger fileName="$(filebase).trees" logEvery="10000">
      <log idref="tree"/>
    </logger>

    <logger fileName="$(filebase)_true.trees" logEvery="10000">
      <log idref="trueTree"/>
    </logger>


    <!-- Screen log -->
    <logger logEvery="10000">
      <log idref="inferredPopSize"/>
    </logger>
    
  </run>
</beast>
